/**
 * Created by duncanogle on 29/06/2017.
 *
 * Put any vendor imports into here and it'll be bundled separately.
 *
 * Why use a file like this? It means we can load things in separately, or specific functions of
 * modules without having to load everything in
 */
import React from 'react';
import { render } from 'react-dom';
import 'babel-polyfill';
import 'whatwg-fetch';